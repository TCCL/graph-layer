/**
 * token/index.js
 *
 * @tccl/graph-layer
 */

const { TokenManager } = require("./manager");
const { TokenEndpoint } = require("./endpoint");

module.exports = {
    TokenManager,
    TokenEndpoint
};
